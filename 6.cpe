@header

#inc e

using namespace std;

int main()
{
    int N;
    cin >> N; cin.ignore();

    var str = to_string( N );
    if(str.length() == 1) {
        println( str );
        return 1;
    }
    str = str.substr( str.length() - 1, str.length() - 1 );
    println( str );
    
    /* Does not work with Negatives */
    // var lastDig = N % 10;

    // println( lastDig );
}