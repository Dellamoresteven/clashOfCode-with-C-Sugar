#inc e

using namespace std;

def int max_of_four( a, b, c, d ){
    return int ( std::max( std::max( a ,b ), std::max( c, d ) ) );
}

int main() {
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);
    var ans = max_of_four(a, b, c, d);
    println(ans);
    
    return 0;
}